export const colors = {
  bg:          "#fdf6e3",   // page background
  cardBg:      "#fff9f0",   // card background
  border:      "#f5e6c8",   // card border
  coral:       "#ff6b6b",   // primary accent
  coralLight:  "#ffaaaa",   // coral border
  brown:       "#3d2b1f",   // dark text
  tan:         "#b8956a",   // muted text
  gold:        "#8b6914",   // nav text
};

export const font = {
  serif:  "'Georgia', serif",
  sans:   "system-ui, sans-serif",
};

// Responsive breakpoints as a helper
export const isMobile = () => window.innerWidth < 640;